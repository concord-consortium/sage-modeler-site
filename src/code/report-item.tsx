import * as React from "react";
import * as ReactDOM from "react-dom";

import "../styles/report-item.sass";

ReactDOM.render(
  <div>
    SageModeler Report Item Placeholder
  </div>,
  document.getElementById("reportItemApp")
);
